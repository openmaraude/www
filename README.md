# www

This Next.js application is the static website of [le.taxi](https://le.taxi).

## Usage

### Run

Run the production version:

```
$> make run
```

To develop:

```
$> npm install
$> npm run dev
```
